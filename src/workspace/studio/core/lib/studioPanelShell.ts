/** Single-column view panel (fills workspace main slot). */
export const STUDIO_VIEW_ROOT_CLASS =
  "relative flex h-full min-h-0 min-w-0 w-full flex-1 flex-col overflow-hidden"

/** Rail + content row — flat (no gutter split / inset panels). */
export const STUDIO_RAIL_LAYOUT_CLASS =
  "flex h-full min-h-0 min-w-0 w-full flex-1 overflow-hidden bg-background"

/** Content beside the organizer rail — same flat chrome as single-column views. */
export const STUDIO_VIEW_CONTENT_CLASS =
  "relative isolate flex min-h-0 min-w-0 flex-1 flex-col overflow-hidden bg-background"
